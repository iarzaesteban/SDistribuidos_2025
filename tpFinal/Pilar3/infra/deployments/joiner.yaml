# joiner.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: joiner
  labels:
    app: joiner
spec:
  replicas: 2
  selector:
    matchLabels:
      app: joiner
  template:
    metadata:
      labels:
        app: joiner
    spec:
      containers:
      - name: joiner
        image: gcr.io/blockchainsd2025/joiner:latest
        env:
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: rabbitmq-username
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: rabbitmq-password
        livenessProbe:
          httpGet:
            path: /healthz
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        ports:
        - containerPort: 5002
        volumeMounts:
        - name: gcp-sa-vol
          mountPath: /etc/gcp
          readOnly: true
      volumes:
      - name: gcp-sa-vol
        secret:
          secretName: gcp-sa-secret
          items:
          - key: key.json
            path: key.json
---
apiVersion: v1
kind: Service
metadata:
  name: joiner
spec:
  selector:
    app: joiner
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5002
  type: ClusterIP
